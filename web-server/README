run the project

APP_ENV=local go run . 

or

for production
docker compose -f docker-compose-production.yaml build --build-arg APP_ENV=production --build-arg PORT=4000 && docker compose -f docker-compose-production.yaml up -d

for local
docker compose -f docker-compose-local.yaml build --build-arg APP_ENV=local --build-arg PORT=4001 && docker compose -f docker-compose-local.yaml up -d

for staging
docker compose -f docker-compose-staging.yaml build --build-arg APP_ENV=staging --build-arg PORT=4002 && docker compose -f docker-compose-staging.yaml up -d
